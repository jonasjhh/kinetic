// Radius of the on-screen circle the disc is held to during the marker
// check, as a fraction of the image's short side. The capture worker
// measures the marker at the same place.
export const MARKER_GUIDE_RADIUS = 0.3;
